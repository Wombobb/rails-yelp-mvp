<div class="container mt-4">
  <h1>Add an Restaurant</h1>
  <%= simple_form_for @restaurant do |f| %>
    <%= f.input :name %>
    <%= f.input :address %>
    <%= f.input :category %>
    <%= f.input :phone_number %>
  <% end %>

  <%= link_to "back to restaurants", restaurants_path, class: "btn btn-primary text-decoration-none mt-4" %>
</div>
